// this component outputs all the fields that it locates in the detected word
// document and displays it alongside a textfield.

// import { extractText } from "office-text-extractor";

import React from "react";

export const TitleFields = () => {
  //     // Extract the text using `async-await`.
  // const text = await extractText('path/to/file')
  // console.log(text)

  // // Extract the text using Promises.
  // extractText('path/to/file')
  // 	.then((text) => console.log(text))
  // 	.catch((error) => console.error(error))
  return <div>TitleFields</div>;
};

//
